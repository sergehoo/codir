# OBSOLÈTE — La prod CODIR utilise désormais Traefik (cf. deploy/traefik/).
# Ce fichier est conservé en lecture seule au cas où on voudrait revenir à
# Nginx. Voir docker-compose.prod.yml + deploy/traefik/traefik.yml.
