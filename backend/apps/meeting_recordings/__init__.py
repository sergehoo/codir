"""Module meeting_recordings — capture micro, transcription, diarisation, IA.

`default_app_config` n'est plus nécessaire depuis Django 3.2 (supprimé en 5.1),
Django découvre `apps.py` automatiquement via INSTALLED_APPS.
"""
